import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        margin: 10
    },
    image: {
        width: 200,
        height: 200,
        borderRadius: 50,
        alignSelf: 'center',
        margin: 20,
        borderColor: '#001f3f',
        borderWidth: 5
    },
    });

export default styles;